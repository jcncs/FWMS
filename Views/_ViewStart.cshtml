@using Microsoft.AspNetCore.Http;
@{
    var currentrole = Context.Session.GetString("Role");
    var role = new List<string> { "1", "2", "3", "4" };

    if (role.Contains(currentrole))
    {
        Layout = "_AdminLayout";
    }
    else
    {
        Layout = "_Layout";
    }
}
